At the moment, the database follows a simple schema.
The main entities being modeled are Products, Customers and Orders.


There are four tables, corresponding to the three entities described above as well as `order_items`, a join-table to reflect the many-to-many relationship between Products and Orders.